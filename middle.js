// Array 1 exists
var Array1 = [16, 18, 39, 24, 37, 263]

// Create an empty Array2 - target of our function
var Array2 = []

// count the number of elements in Array1
var len = Array1.length

// check what element will be the middle element; if the number of elements in array is odd, round it up
var mid = Math.ceil(len)

// checking parity of array length
function(n) {
    if len - mid
    n = "even"
    else n = "odd"
}

// filling Array2 with the middle elements of Array1; 1 if len is odd, or 2 if len is even
// add to Array2 - Array1[mid-1]
// then if n = "even"
// add to Array2 - Array1[len-mid-1]
// return Array2

console.log(middle)